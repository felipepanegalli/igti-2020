const databasePath = 'src/database/';

const db = {
  states: databasePath + 'Estados.json',
  cities: databasePath + 'Cidades.json',
  cityById: databasePath + 'cityByState.json',
};

export default db;
